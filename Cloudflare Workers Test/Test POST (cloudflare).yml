info:
  name: Test POST (cloudflare)
  type: http
  seq: 3

http:
  method: POST
  url: https://karasu-cf-worker-test.karasu-account.workers.dev/?test=thingy
  params:
    - name: test
      value: thingy
      type: query
  body:
    type: json
    data: |
      {
        "name": "Fred",
        "email": "hello@nope",
        "message": "This is a test message"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
