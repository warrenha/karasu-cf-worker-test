info:
  name: Kuromoji API
  type: http
  seq: 3

http:
  method: POST
  url: https://api.aiktb.com/tokenizer
  body:
    type: json
    data: |
      {
        "text": "寿司"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
