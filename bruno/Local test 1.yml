info:
  name: Local test 1
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8787?test=thing
  params:
    - name: test
      value: thing
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
