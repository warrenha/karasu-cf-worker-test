info:
  name: Test GET
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8787/?test=bob
  params:
    - name: test
      value: bob
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
